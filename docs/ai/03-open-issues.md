# Open issues

Secondary issues only; these are not the current first-loss:

- `CHILD_RUNTIME_TELEMETRY_PREFIXES` todavía no propaga promoted-press traces.
- `functionalExecution` outer error pierde exception message y conserva principalmente screenshot path.
